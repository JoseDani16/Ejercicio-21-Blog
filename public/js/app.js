/**
 * Colocar aquí JS "propio".
 * Notar que este código se ejecutará en el navegador.
 */

//let limitContent = document.querySelectorAll("td");

/*
for (let i = 0; i < limitContent.length; i++) {
  let contenido = limitContent[i].innerHTML;
  console.log(contenido);

  if (contenido.length > 50) {
    contenido = contenido.slice(0, 150) + "pepito";
  }
  limitContent[i].innerHTML = contenido;
}
*/
