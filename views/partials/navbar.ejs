<nav class="navbar navbar-dark bg-dark">
  <div class="container justify-content-around">
    <a class="navbar-brand fs-5 fw-bold" href="/">Hack the blog</a>
    <div>
      <% if(!user) {%>
      <a class="btn btn-primary" href="/login">Login</a>
      <a class="btn btn-success" href="/usuarios/crear">Register</a>
      <% } else { %>
      <div class="dropdown">
        <button
          class="bg-transparent text-white dropdown-toggle"
          style="border: none"
          type="button"
          data-bs-toggle="dropdown"
          aria-expanded="false"
        >
          <%=user.firstname+' '+user.lastname %>
        </button>
        <ul class="dropdown-menu m-0 py-1">
          <%if (user.levelPermission >= 2) {%>
          <li>
            <a href="/admin" disabeled class="dropdown-item">Admin</a>
            <hr class="dropdown-divider" />
          </li>
          <%}%> <%if (user.levelPermission === 4) {%>
          <li>
            <a href="/admin/users" disabeled class="dropdown-item">Administracion de Usuarios</a>
            <hr class="dropdown-divider" />
          </li>
          <%}%>
          <li><a href="/admin/logout" class="dropdown-item">Logout</a></li>
        </ul>
      </div>
      <%}%>
    </div>
  </div>
</nav>
